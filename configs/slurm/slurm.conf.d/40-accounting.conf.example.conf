# 40-accounting.conf.example
# Example only. Replace placeholders before production.

# Job completion logging
JobCompType=jobcomp/<JOBCOMP_TYPE>          # e.g. filetxt
JobCompLoc=<JOBCOMP_LOC>                    # e.g. /var/log/slurm/slurm-job-completions.txt

# Job accounting gather
JobAcctGatherType=jobacct_gather/<GATHER>   # e.g. linux

# Unkillable step timeout (seconds)
UnkillableStepTimeout=<SECONDS>             # e.g. 300

# Default resource mapping per GPU (example)
DefCPUPerGPU=<CPUS_PER_GPU>                 # e.g. 16
DefMemPerGPU=<MEM_MB_PER_GPU>               # e.g. 200000  (MB)

# Scheduler / priority (example)
SchedulerType=sched/<SCHED_TYPE>            # e.g. backfill
PriorityType=priority/<PRIORITY_TYPE>       # e.g. multifactor

###############################################################################
# Accounting / SlurmDBD integration (EXAMPLE)
###############################################################################
# SlurmDBD:
# - slurmdbd should be running and reachable
# - slurmctld will write accounting records via slurmdbd

# Enable Slurm accounting
AccountingStorageType=accounting_storage/slurmdbd
AccountingStorageHost=<SLURMDBD_HOST>      # e.g. localhost or slurmdbd.internal
AccountingStoragePort=<PORT>               # e.g. 6819

# Trackable resources (TRES)
# Common set: cpu,mem,energy,node,billing,gres/gpu
AccountingStorageTRES=cpu,mem,energy,node,billing,gres/gpu

# Policy enforcement (examples)
# - limits: enforce resource limits
# - qos: enforce QOS rules
# - associations: enforce user/account associations
AccountingStorageEnforce=limits,qos,associations

# Optional: job accounting gather plugin (common on Linux)
JobAcctGatherType=jobacct_gather/linux

# Priority weights (example)
PriorityWeightPartition=<INT>
PriorityWeightQOS=<INT>
PriorityWeightFairshare=<INT>